# Core Numerical Foundations
numpy>=1.24.0

# Classical Machine Learning Backend
scikit-learn>=1.2.0

# Deep Learning Framework
torch>=2.0.0

# Intel Hardware Acceleration (Optional Fallbacks)
# Note: install via alternative channels (e.g. intel channel on conda) if specific matching wheels are missing
scikit-learn-intelex>=2023.1.0
dpctl>=0.14.0; sys_platform == 'linux' or sys_platform == 'win32'
intel-extension-for-pytorch>=2.0.0; sys_platform == 'linux' or sys_platform == 'win32'